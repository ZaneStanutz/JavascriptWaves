import type { RGB, RGBA } from './interfaces.js';
/**
 * Convert `RGB` to `#rgb`
 * JavaScript note: no check for array length, use it properly.
 */
export declare function rgbHex(rgb: RGB | RGBA): string;
